{
  "type": "object",
  "properties": {
    "identifier": {
      "type": "string",
      "enum": [
        "cdrs",
        "commands",
        "credentials",
        "locations",
        "sessions",
        "tariffs",
        "tokens",
		"chargingprofiles"      
		]
    },
    "role": {
      "type": "string",
      "enum": [
        "SENDER",
        "RECEIVER"
      ]
    },
    "url": {
      "type": "string",
      "format": "uri"
    }
  },
  "require": [
    "identifier",
    "role",
    "url"
  ],
  "additionalProperties": false
}
